<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
      <a href="/logout">Log Out</a>

      <% if Helper.is_logged_in?(session) %>
        <h3>Hello, <%= @user.username %>!</h3>
        <h4>Your current balance is $<%= @user.balance.to_s %>.</h4>

  </body>
</html>
