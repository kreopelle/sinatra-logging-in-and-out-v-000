<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
      <a href="/logout">Log Out</a>

      <% if Helper.is_logged_in?(session) %>
        <h1>Welcome <%= Helper.current_user(session).username %></h1>
        <h3>Your balance: <%= Helper.current_user(session).balance.to_s %>.</h4>
      <% else %>
        <h3>You Must <a href="/">Log In</a> to View Your Balance</h3>
      <% end %>

  </body>
</html>
